#include "main.h"
/**
 * mul - used to multiply two integers
 * @m: first integer
 * @n: second integer
 * Return: the multiple of first and second integer
 */
int mul(int m, int n)
{
	return (m * n);
}
