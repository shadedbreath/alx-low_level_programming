#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strlen - determines the length of a null-terminated string
 * @s: pointer to the null-terminated string
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
