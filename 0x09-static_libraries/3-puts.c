#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _puts - prints string followed by a newline character
 * @s: pointer to the null-terminated string
 *
 * Return: successful always
 */

void _puts(char *s)
{
	puts(s);
}
