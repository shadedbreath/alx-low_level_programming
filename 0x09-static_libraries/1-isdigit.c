#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - determines whether a given character is a decimal digt or not
 * @c: the character to be tested
 *
 * Return: an integer value
 */

int _isdigit(int c)
{
	return (isdigit(c));
}
